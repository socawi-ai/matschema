# matschema

Starter framework for a Node.js + Express web application.

## Run locally

1. Install dependencies:
   `npm install`
2. Start server:
   `npm start`
3. Open:
   `http://localhost:3000`

Health endpoint:
`http://localhost:3000/health`
